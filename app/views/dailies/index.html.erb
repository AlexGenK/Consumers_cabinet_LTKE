<div class="card">
	<div class="card-header">
		<i class="fas fa-plug"></i> <b><%= @consumer.name %></b>
	</div>
	<div class="card-body">
		<div class='row'>
			<div class='col'>
				<%= link_to consumer_monthlies_path(@consumer), class: "btn btn-outline-dark" do %>
            		<i class="fas fa-undo-alt"></i> Повернутись
        		<% end %>
			</div>
		</div>
	</div>
</div>